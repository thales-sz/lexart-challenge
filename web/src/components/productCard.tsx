import React from 'react'
import { type IProduct } from '../context/Provider'

function ProductCard ({ web, category, description, image }: IProduct): JSX.Element {
  return (
    <div>productCard</div>
  )
}

export default ProductCard
